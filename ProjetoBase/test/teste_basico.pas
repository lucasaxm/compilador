program exemplo75 (input, output);
    var b:integer; a:boolean;
begin
    b:=0;
    while (b<10) do
        if (a=true) then
            while (a) do
                a:=false
        else if (b>10) then
            b:=10
        else
            a := b=10;
end.

